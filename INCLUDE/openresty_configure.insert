
    # add luarocks
    push @make_install_cmds, "cd $root_dir/build/luarocks-benchmark* && " .
        "\$(MAKE) install && cd \$(DESTDIR)/var/lib/nginx/luajit/bin && ln -s luajit* luajit";

    # and some more commands
